{
  "name": "landingpage-portifolio",
  "version": "1.0.0",
  "description": "Portfólio Jonnathan Quintela",
  "scripts": {
    "dev": "npx live-server . --port=3000 --open",
    "start": "npx live-server . --port=3000 --open",
    "serve": "npx live-server . --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}